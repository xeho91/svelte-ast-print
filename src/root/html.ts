import type { Root } from "svelte/compiler";

import type { TransformedPrintOptions } from "#options";

export function print_html(node: Root["fragment"], options: TransformedPrintOptions): string {
	return "WIP";
}
